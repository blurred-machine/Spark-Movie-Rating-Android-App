package com.example.paras.imdb;

// an interface used to transfer the data form the FetchData class and use it in the FirstClass.
public interface DataTransferInterface {
    void dataHere(String data);
}
